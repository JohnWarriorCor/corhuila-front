import { FiltroSexoBiologicoPipe } from './filtro-sexo-biologico.pipe';

describe('FiltroSexoBiologicoPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroSexoBiologicoPipe();
    expect(pipe).toBeTruthy();
  });
});
