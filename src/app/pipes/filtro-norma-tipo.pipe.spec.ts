import { FiltroNormaTipoPipe } from './filtro-norma-tipo.pipe';

describe('FiltroNormaTipoPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroNormaTipoPipe();
    expect(pipe).toBeTruthy();
  });
});
