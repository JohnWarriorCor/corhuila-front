import { FiltroMunicipioPipe } from './filtro-municipio.pipe';

describe('FiltroMunicipioPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroMunicipioPipe();
    expect(pipe).toBeTruthy();
  });
});
