import { FiltroMedioPipe } from './filtro-medio.pipe';

describe('FiltroMedioPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroMedioPipe();
    expect(pipe).toBeTruthy();
  });
});
