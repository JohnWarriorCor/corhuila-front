import { FiltroCampoAmplioPipe } from './filtro-campo-amplio.pipe';

describe('FiltroCampoAmplioPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroCampoAmplioPipe();
    expect(pipe).toBeTruthy();
  });
});
