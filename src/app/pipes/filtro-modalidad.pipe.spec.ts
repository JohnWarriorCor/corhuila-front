import { FiltroModalidadPipe } from './filtro-modalidad.pipe';

describe('FiltroModalidadPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroModalidadPipe();
    expect(pipe).toBeTruthy();
  });
});
