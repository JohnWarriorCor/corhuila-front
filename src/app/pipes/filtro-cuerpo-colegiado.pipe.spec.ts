import { FiltroCuerpoColegiadoPipe } from './filtro-cuerpo-colegiado.pipe';

describe('FiltroCuerpoColegiadoPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroCuerpoColegiadoPipe();
    expect(pipe).toBeTruthy();
  });
});
