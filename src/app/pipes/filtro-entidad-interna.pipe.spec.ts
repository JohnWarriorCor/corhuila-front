import { FiltroEntidadInternaPipe } from './filtro-entidad-interna.pipe';

describe('FiltroEntidadInternaPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroEntidadInternaPipe();
    expect(pipe).toBeTruthy();
  });
});
