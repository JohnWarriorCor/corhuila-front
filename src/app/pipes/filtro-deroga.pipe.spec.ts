import { FiltroDerogaPipe } from './filtro-deroga.pipe';

describe('FiltroDerogaPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroDerogaPipe();
    expect(pipe).toBeTruthy();
  });
});
