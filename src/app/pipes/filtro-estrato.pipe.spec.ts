import { FiltroEstratoPipe } from './filtro-estrato.pipe';

describe('FiltroEstratoPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroEstratoPipe();
    expect(pipe).toBeTruthy();
  });
});
