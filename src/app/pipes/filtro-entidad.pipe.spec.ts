import { FiltroEntidadPipe } from './filtro-entidad.pipe';

describe('FiltroEntidadPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroEntidadPipe();
    expect(pipe).toBeTruthy();
  });
});
