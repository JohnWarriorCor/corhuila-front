import { FiltroCampoDetalladoPipe } from './filtro-campo-detallado.pipe';

describe('FiltroCampoDetalladoPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroCampoDetalladoPipe();
    expect(pipe).toBeTruthy();
  });
});
