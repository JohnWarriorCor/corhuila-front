import { FiltroEstadoSniesPipe } from './filtro-estado-snies.pipe';

describe('FiltroEstadoSniesPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroEstadoSniesPipe();
    expect(pipe).toBeTruthy();
  });
});
