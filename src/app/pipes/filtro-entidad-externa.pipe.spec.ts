import { FiltroEntidadExternaPipe } from './filtro-entidad-externa.pipe';

describe('FiltroEntidadExternaPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroEntidadExternaPipe();
    expect(pipe).toBeTruthy();
  });
});
