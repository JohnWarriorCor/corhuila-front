import { FiltroCampoEspecificoPipe } from './filtro-campo-especifico.pipe';

describe('FiltroCampoEspecificoPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroCampoEspecificoPipe();
    expect(pipe).toBeTruthy();
  });
});
